[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "physicsnemo-meshgraphnet-surface"
version = "1.0.0"
description = "Portable PhysicsNeMo MeshGraphNet surface-field experiment."
requires-python = ">=3.11,<3.13"

[tool.setuptools.packages.find]
where = ["src"]

